{
  "git": {
    "tagName": "v${version}",
    "commitMessage": "release v${version}",
    "push": true,
    "pushArgs": "--follow-tags"
  },
  "github": {
    "release": true,
    "releaseName": "v${version}"
  }
}